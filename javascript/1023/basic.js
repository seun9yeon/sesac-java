// let x = 10;
// 정의 된 함수에 중
// function mul2(num) {
//   let mul2num = num * 2;
//   내부영역을 정리
// }
// return x * 2;

//1. 일단 함수의 틀을 만든다.

// function mul2(two) {
//   //실행할 코드
//   const a = two * 2;

//   //2를 곱하는 함수를 만든다.
//   // * 2
//   return a;
// }
// console.log(mul2(4));

// let
// 값의 재할당 가능
// const

//2. 하고싶은 일을 서술한다.

//2-1. 어떤 값을 넣어야 이 함수를 실행 시킬 수 있을까?
//2-1. 함수를 실행시키려면 어떤 값이 필요할까?

//2-2. 이 함수는 어떤 걸 return(결과값)해야할까?

// function checkPrime(num) {
//  let isPrime = true;
//  for (let i = 2; i < num; i++) {
//   if (num % 1 === 0) {
//     isPrime = flase;
//     return isPrime
//   }
//  }
//   retun isPrime
// }
// 1. 2를 곱하는 함수를 만들어보자.

// 2. 소수를 판별하는 함수를 만들어보자.

// 3. 구구단 중 n단을 return하는 함수를 만들어보자.
