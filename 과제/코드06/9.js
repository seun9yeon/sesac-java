// 변수 username, password 선언 및 값 할당
let username = 'sesac'; // 임의의 값
let password = 'sesac1234'; // 임의의 값

// 변수 값 출력
console.log(username);
console.log(password);

// 조건문: username과 password 일치 여부 확인
if (username === 'sesac' && password === 'sesac1234') {
  console.log('로그인 성공');
} else {
  console.log('로그인 실패');
}
